spring:
  application:
    name: server
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
    mcp:
      server:
        name: codemash-mcp-server
        version: 1.0.0
        type: SYNC
        protocol: streamable # sse, stateless or streamable
server:
  port: 8081